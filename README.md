# Meal Splitter
* A quick program I made to automatically split meals for me when I go out to a restaurant and pay for the bill

# Usage
* Fill out names in `people.js` and `npm run template`
* Fill out information in `receipt.js` and `npm run split`